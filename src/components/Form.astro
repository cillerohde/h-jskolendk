<div class="bg-gradient-to-b from-blue-100 to-white p-6 rounded-xl shadow-md space-y-4 max-w-xl mx-auto">
  <div>
    <label class="font-semibold">HØJSKOLEOPHOLD:</label>
    <select class="select select-bordered w-full mt-1">
      <option disabled selected>Vælg ønsket destination her</option>
      <option>Spanien</option>
      <option>Frankrig</option>
      <option>Danmark</option>
    </select>
  </div>

  <div>
    <label class="font-semibold">KONTAKTINFO:</label>
    <input type="text" placeholder="Dit fulde navn" class="input input-bordered w-full mt-1" />
  </div>

  <div>
    <label class="font-semibold">VÆLG KØN: <span class="text-sm font-normal">(som det fremgår i dit pas)</span></label>
    <select class="select select-bordered w-full mt-1">
      <option disabled selected>Vælg køn</option>
      <option>Mand</option>
      <option>Kvinde</option>
      <option>Andet</option>
    </select>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
    <input type="tel" placeholder="Tlf.nr" class="input input-bordered w-full" />
    <input type="email" placeholder="Email" class="input input-bordered w-full" />
  </div>

  <textarea class="textarea textarea-bordered w-full" rows="3" placeholder="Bemærkning..."></textarea>

  <div class="space-y-2 pt-2">
    <p class="font-semibold text-red-500">*Disse felter skal udfyldes/vælges</p>

    <div class="form-control">
      <label class="label cursor-pointer">
        <input type="checkbox" class="checkbox checkbox-primary" />
        <span class="label-text ml-2">Jeg har læst jeres privatlivspolitik og generelle bestemmelser og er indforstået med begge*</span>
      </label>
    </div>

    <div class="form-control">
      <label class="label cursor-pointer">
        <input type="checkbox" class="checkbox checkbox-primary" />
        <span class="label-text ml-2">Ja tak - send mig jeres brochure</span>
      </label>
    </div>
  </div>
</div>
